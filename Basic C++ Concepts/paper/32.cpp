#include <iostream>
using namespace std;

void function(char array[])
{
	bool flag=true;
	char max[27];
	for (int i=0 ; array[i]!='\0' ; i++)
	{
		if (array[i]>='A' && array[i]<='Z')
		{
			array[i]
		}

		if (array[i]>='a' && array[i]<='a')
		{
			flag=true;
		}
	
